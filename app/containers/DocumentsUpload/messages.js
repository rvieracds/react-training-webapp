/*
 * DocumentsUpload Messages
 *
 * This contains all the text for the DocumentsUpload container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DocumentsUpload';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentsUpload container!',
  },
});
