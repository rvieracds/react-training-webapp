/*
 * DocumentsListItem Messages
 *
 * This contains all the text for the DocumentsListItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DocumentsListItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentsListItem component!',
  },
});
