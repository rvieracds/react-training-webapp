/*
 * DocumentsUploadHeader Messages
 *
 * This contains all the text for the DocumentsUploadHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DocumentsUploadHeader';

export default defineMessages({
  headerTitle: {
    id: `${scope}.headerTitle`,
    defaultMessage: 'Upload documents and increase your score',
  },
});
